import React from 'react'
import { Input } from '@freeformation/ds-components'

const meta = {
  title: 'Atoms/Input',
  component: Input,
}

export default meta

export const Default = {
  args: {
    placeholder: 'Type here',
  },
}
