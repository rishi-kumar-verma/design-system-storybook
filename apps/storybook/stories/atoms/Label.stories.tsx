import React from 'react'
import { Label } from '@freeformation/ds-components'

const meta = {
  title: 'Atoms/Label',
  component: Label,
}

export default meta

export const Default = {
  args: {
    children: 'Name',
  },
}
