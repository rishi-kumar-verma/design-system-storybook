import React from 'react'
import { Button } from '@freeformation/ds-components'

const meta = {
  title: 'Atoms/Button',
  component: Button,
}

export default meta

export const Primary = {
  args: {
    children: 'Click me',
  },
}
